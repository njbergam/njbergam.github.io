# njbergam.github.io

Personal academic website — Noah Bergam (Columbia CS PhD).

Built with [Jekyll](https://jekyllrb.com/). To run locally:

```bash
bundle install
bundle exec jekyll serve
```

Then open http://localhost:4000.
